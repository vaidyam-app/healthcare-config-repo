server:
  port: 8081
  servlet:
    context-path: /patient

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/patient_db
    username: postgres
    password: admin

  jpa:
    hibernate:
      ddl-auto: update
