server:
  port: 8080

spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id: patient-service
          uri: http://localhost:8081
          predicates:
            - name: Path
              args:
                pattern: /patients/**
          filters:
            - name: RewritePath
              args:
                regexp: /patients/(?<segment>.*)
                replacement: /$\{segment}

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
